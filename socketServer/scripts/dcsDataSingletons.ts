type DcsData = { [address: string]: number };
type PreviousUpdates = { [identifier: string]: string };

const dcsData: DcsData = {};
const previousUpdates: PreviousUpdates = {};

export { dcsData, previousUpdates };
